using UnityEngine;
using System.Collections;

public class DamierFlipper : MonoBehaviour {
  /*  public int BPM = 
  ;

    private GameObject[] Dam1;
    private GameObject[] Dam2;

    private float timer;
	// Use this for initialization
	void Start () {

        


        timer = 1.0f / (BPM / 60.0f);
    }
	
	// Update is called once per frame
	void Update () {
        if (timer > 0.0f)
            timer -= Time.deltaTime;
	if(timer<=0.0f)
        {

            gameObject.transform.RotateAround(transform.position, new Vector3(1.0f, 0.0f, 0.0f),180.0f);
           
            timer = 1.0f / (BPM / 60.0f);
        }

	}*/
}
